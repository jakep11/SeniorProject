# Example YAML to get you started quickly.
# Be aware that YAML has indentation based scoping.
# Code completion support is available so start typing for available options.
swagger: '2.0'

# This is your document metadata
info:
  version: "0.0.0"
  title: Senior Project REST Spec

# Describe your paths here
paths:
  # This is a path endpoint. Change it.
  /user:
    # This is a HTTP operation
    get:
      # Describe this verb here. Note: you can use markdown
      description: |
        Gets `User` objects.
        Optional query param of **email** determines limits response to `Users` with specified email or email prefix.
      # This is array of GET operation parameters:
    #  parameters:
        # An example parameter that is in query and is required
    #    -
    #      name: size
    #      in: query
    #      description: Size of array
     #     required: true
     ##     format: double
      # Expected responses for this operation:
      responses:
        # Response code
        200:
          description: Successful response
          # A schema describing your response object.
          # Use JSON Schema format
          schema:
            title: ArrayOfPersons
            type: array
            items:
              title: Person
              type: object
              properties:
                email:
                  type: string
                  description: principal string identifier, unique across all Users
                id:
                  type: number
                  description: id of person with said email

  /topic:
    get:
      description: |
        Gets a list of all `Topics`
    post:
      description: |
        Creates a new topic. Requires admin.
  /userx:
    # This is a HTTP operation
    get:
      # Describe this verb here. Note: you can use markdown
      description: |
        Gets `User` objects.
        Optional query param of **email** determines limits response to `Users` with specified email or email prefix.
      # This is array of GET operation parameters:
    #  parameters:
        # An example parameter that is in query and is required
    #    -
    #      name: size
    #      in: query
    #      description: Size of array
     #     required: true
     ##     format: double
      # Expected responses for this operation:
      responses:
        # Response code
        200:
          description: Successful response
          # A schema describing your response object.
          # Use JSON Schema format
          schema:
            title: ArrayOfPersons
            type: array
            items:
              title: Person
              type: object
              properties:
                email:
                  type: string
                  description: principal string identifier, unique across all Users
                id:
                  type: number
                  description: id of person with said email

  /usery:
    # This is a HTTP operation
    get:
      # Describe this verb here. Note: you can use markdown
      description: |
        Gets `User` objects.
        Optional query param of **email** determines limits response to `Users` with specified email or email prefix.
      # This is array of GET operation parameters:
    #  parameters:
        # An example parameter that is in query and is required
    #    -
    #      name: size
    #      in: query
    #      description: Size of array
     #     required: true
     ##     format: double
      # Expected responses for this operation:
      responses:
        # Response code
        200:
          description: Successful response
          # A schema describing your response object.
          # Use JSON Schema format
          schema:
            title: ArrayOfPersons
            type: array
            items:
              title: Person
              type: object
              properties:
                email:
                  type: joeadmin@domain.com
                  description: principal string identifier, unique across all Users
                id:
                  type: 1
                  description: id of person with said email




